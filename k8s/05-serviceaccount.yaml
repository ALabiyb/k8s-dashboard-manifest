# ── ServiceAccount ────────────────────────────────────────────────────────
# A dedicated identity for the dashboard pod — never use the default SA
apiVersion: v1
kind: ServiceAccount
metadata:
  name: k8s-dashboard
  namespace: k8s-dashboard
